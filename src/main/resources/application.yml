spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/uptime
    username: uptime
    password: uptime
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
  flyway:
    enabled: true
server:
  port: 8080
